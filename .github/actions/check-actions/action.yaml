---
runs:
  steps:
  - uses: actions/checkout@v4
    with:
      sparse-checkout: |
        .github/actions
  - env:
    run: 'sh ${{ github.action_path }}/sh/check-actions.sh'
    shell: 'sh'
  using: 'composite'
...
